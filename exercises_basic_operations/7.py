print(int('3.1415')) # What will the output be?

# I thought it would return 3.0 since we're using the integer type coercion.

# Turns out a ValueError is raised since its a string value and does not represent a valid integer.